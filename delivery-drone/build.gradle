def mainClass = 'gpig.drones.delivery.DeliveryDrone'

mainClassName = mainClass

dependencies {
    compile project(':common')
}

jar {
    manifest {
        attributes 'Main-Class': mainClass
    }
}
