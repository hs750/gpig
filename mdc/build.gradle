def mainClass = 'gpig.mdc.MDC'

mainClassName = mainClass

dependencies {
}

jar {
    manifest {
        attributes 'Main-Class': mainClass
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
